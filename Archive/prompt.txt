I wanted to create a notebook that goes from Markdown to code that describes the math behind a convolutional neural network. We're going to be working with a simple task. I want to first show what computer vision is in simple terms. As little text as possible in the markdowns, and bullet everything. Make it look very neat and not AI-generated. 

We wanted to talk about what computer vision is and what the computer actually sees. 

Side by side, on the left, I want you to show a highly pixelated picture of a cat. On the right-hand side, I want you to show the same picture, but in pixel values, and overlay red, blue, red, green, and blue matrices on top of each other. You should also zoom into the pixels and see clearly that what a computer sees is actually a matrix of numbers. On the left, a highly pixelated picture of a cat; on the right, just a matrix of numbers, same dimensions 

I also wanted to then talk about how this data gets loaded onto a tensor and describe a little bit what a tensor is. 

Show some Python code for how to load a picture onto a tensor. 

Then we're going to have another image that describes what a neural network is and how you put a picture into a node that then does a convolution. One layer first 

Add the math behind the convolution, but also describe each term of the math in Markdown. 

Next, we're going to load a picture of a cat and import a neural network using PyTorch that loads a traditional convolutional neural network. Please also feel free to find an open-source dataset and a pre-trained neural network that has the capacity to be fine-tuned for discerning cats and dogs. I want this to be a binary classification problem. 

I also want you to show how a zero relates to a cat and a one relates to a dog in a picture in the next markdown. 

After that, I want you to show me:
- what backpropagation is in very simple terms
- what a training epoch is
- what the basic parameters are that you fine-tune to train a neural network

Next, I want it to be a convolutional neural network that gets loaded and trained in a simple loop that I can run. 

Last thing: I wanted to show another picture where you load a test set of another cat that you haven't seen before and ask the neural network in the code to predict whether it's a cat or a dog. Feel free to show as much information about the soft scores and hidden variables and stuff like that. 

And that's it. I want you to keep it as clean as possible, but make a functional neural network. Thank you.